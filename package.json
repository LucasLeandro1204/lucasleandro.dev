{
  "name": "l17s",
  "version": "0.0.1",
  "description": "We are L17S. We solve problems.",
  "author": "LucasLeandro1204",
  "private": true,
  "scripts": {
    "prebuild": "test \"$CI\" = true && npx pnpm install -r --store=node_modules/.pnpm-store",
    "bootstrap": "pnpm build -r"
  },
  "devDependencies": {
    "pnpm": "^6.0.1"
  }
}
